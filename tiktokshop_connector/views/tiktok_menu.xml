<odoo>

    <!-- ROOT (HIỆN Ở MENU 3 SỌC) -->
    <menuitem id="menu_tiktok_root"
              name="TikTok"
              sequence="10"
              web_icon="tiktokshop_connector,static/description/icon.png"/>

    <!-- ACTION -->
    <record id="action_tiktok_order" model="ir.actions.act_window">
        <field name="name">TikTok Orders</field>
        <field name="res_model">tiktok.order</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- MENU CON -->
    <menuitem id="menu_tiktok_order"
              name="Orders"
              parent="menu_tiktok_root"
              action="action_tiktok_order"/>

</odoo>